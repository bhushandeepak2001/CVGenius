
AI-Powered ATS Resume Builder

How to Run:

1. Install dependencies:
   pip install -r requirements.txt

2. Run server:
   uvicorn main:app --reload

Endpoints:
- GET /
- POST /upload-resume/
- POST /generate-resume/

This is a starter template. Integrate OpenAI, Gemini, and ATS APIs inside generate_resume().
